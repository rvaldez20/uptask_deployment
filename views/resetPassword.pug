<!DOCTYPE html>
html(lang="en")
   head
      include partials/header
   body.login
      .contenedor-formulario
         h1= nombrePagina
         p Coloca tu nuevo Password

         form.caja-login(method="POST")
            .campo
               label(for="password") Password:
               input(type="password" name="password" placeholder="Nuevo Password" value= password)
            .campo.enviar
               input(type="submit" class="boton boton" value="Restablecer Password")



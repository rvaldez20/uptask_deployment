extends layout

include mixins/crearYeditarProyecto

block contenido
   .contenido-principal
      h1 #{nombrePagina}

      //- pre= vardump(proyecto)

      if errores
         each error in errores
            .alerta.error= error.texto
      +crearYeditarProyecto(proyecto)

      